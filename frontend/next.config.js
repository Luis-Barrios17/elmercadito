/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['tienditasistemas1-app-e9752b9ed618.herokuapp.com'], // Agrega aquí los dominios permitidos
  },
};

export default nextConfig;
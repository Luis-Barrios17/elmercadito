import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ffffff', // Color blanco
    },
    secondary: {
      main: '#b31e23', // Color rojo
    },
  },
});

export default theme;